// Static adapter config: this app runs client-side in Tauri/Capacitor webviews
// or as a static SPA on the web. No SSR needed.
export const prerender = true;
export const ssr = false;
export const trailingSlash = 'always';
